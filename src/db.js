const connection = mysql.createConnection({
    host: '127.0.0.1', // instead of 'localhost'
    user: 'root',
    password: '', // default in XAMPP is usually empty
    database: 'VirtualTrainR'
  });
  